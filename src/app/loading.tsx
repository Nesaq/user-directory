/**
 * Компонент загрузки для страниц приложения.
 * Возвращаю null, так как индикация загрузки реализована через opacity в UserList.
 *
 * Next.js автоматически использует этот компонент во время загрузки страницы.
 * Но я сделал собственную реализацию так:
 * - Текущий список пользователей остается видимым, но становится полупрозрачным
 * - Кнопки пагинации блокируются
 * - Это создает более плавный UX без резких изменений макета
 */
export default function Loading() {
  return null; // Возвращаем null, так как мы используем opacity для индикации загрузки
}
